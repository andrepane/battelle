import test from 'node:test';
import assert from 'node:assert/strict';
import { loadJson } from '../src/battelle-data.js';
import { loadScaleModel } from '../src/battelle-scales.js';
import { lookupPercentile, percentileForScale, lookupEquivalentAge, validatePercentileMappings, PERCENTILE_SCALE_NAMES } from '../src/battelle-conversions.js';
const percentiles = await loadJson('data/percentiles_battelle.json');
const ages = await loadJson('data/edades_equivalentes.json');
const model = await loadScaleModel();
for (const [age, pd, pct] of [[0,18,98],[6,22,92],[12,35,98],[18,36,93],[24,36,86]]) test(`percentil selecciona tramo ${age}`,()=>assert.equal(lookupPercentile({ageMonths:age,scaleName:'Interacción con el adulto',directScore:pd,percentileData:percentiles}).percentile,pct));
test('mapeos de percentiles cubren 22 subáreas y nombres existentes',()=>{ assert.doesNotThrow(()=>validatePercentileMappings(model, percentiles)); assert.equal(Object.keys(model.subareas).every(id=>Object.hasOwn(PERCENTILE_SCALE_NAMES,id)),true); const names=new Set(percentiles.tramos.flatMap(t=>t.registros.map(r=>r.escala))); for(const id of Object.keys(model.subareas)) assert.equal(names.has(PERCENTILE_SCALE_NAMES[id]),true,id); });
test('subáreas: percentil real, no aplicable, null, revisión y Battelle total sin percentil',()=>{ assert.equal(percentileForScale({ageMonths:0,scaleId:'personal_social_interaccion_con_el_adulto',directScore:18,percentileData:percentiles}).ok,true); assert.equal(percentileForScale({ageMonths:0,scaleId:'personal_social_rol_social',directScore:1,percentileData:percentiles}).error.code,'escala_no_encontrada'); assert.equal(percentileForScale({ageMonths:0,scaleId:'personal_social_interaccion_con_el_adulto',directScore:null,percentileData:percentiles}).error.code,'pd_invalida'); assert.equal(percentileForScale({ageMonths:0,scaleId:'personal_social_interaccion_con_el_adulto',directScore:18,percentileData:percentiles,requiresReview:true}).error.code,'requiere_revision'); assert.equal(percentileForScale({ageMonths:0,scaleId:'battelle_total',directScore:10,percentileData:percentiles}).error.code,'escala_sin_percentil'); });
test('percentiles: límites, abierto, sin coincidencia, doble, escala inexistente y 36-47',()=>{ assert.equal(lookupPercentile({ageMonths:0,scaleName:'Interacción con el adulto',directScore:18,percentileData:percentiles}).ok,true); assert.equal(lookupPercentile({ageMonths:0,scaleName:'Interacción con el adulto',directScore:36,percentileData:percentiles}).ok,true); assert.equal(lookupPercentile({ageMonths:0,scaleName:'Interacción con el adulto',directScore:99,percentileData:percentiles}).error.code,'sin_coincidencia'); const dup=structuredClone(percentiles); dup.tramos[0].registros.push({...dup.tramos[0].registros[0]}); assert.equal(lookupPercentile({ageMonths:0,scaleName:'Interacción con el adulto',directScore:18,percentileData:dup}).error.code,'coincidencia_multiple'); assert.equal(lookupPercentile({ageMonths:0,scaleName:'No existe',directScore:1,percentileData:percentiles}).error.code,'escala_no_encontrada'); assert.equal(lookupPercentile({ageMonths:36,scaleName:'Interacción con el adulto',directScore:1,percentileData:percentiles}).error.code,'tramo_no_disponible'); });
test('PD inválida se rechaza sin coerción',()=>{ for(const value of ['18',18.5,Number.NaN,Infinity,-1]){ assert.equal(lookupPercentile({ageMonths:0,scaleName:'Interacción con el adulto',directScore:value,percentileData:percentiles}).error.code,'pd_invalida'); assert.equal(lookupEquivalentAge({scaleId:'battelle_total',directScore:value,equivalentAgeData:ages}).error.code,'pd_invalida'); } });
for (const [pd, m] of [[386,37],[421,41],[436,43],[464,47],[537,57],[562,60]]) test(`N-65 ${pd} -> ${m}`,()=>assert.equal(lookupEquivalentAge({scaleId:'battelle_total',directScore:pd,equivalentAgeData:ages}).minMonths,m));
test('edades equivalentes: intervalo, excluidas, sin y doble coincidencia',()=>{ const top=lookupEquivalentAge({scaleId:'battelle_total',directScore:682,equivalentAgeData:ages}); assert.equal(top.minMonths,90); assert.equal(top.maxMonths,95); assert.equal(lookupEquivalentAge({scaleId:'screening_total',directScore:1,equivalentAgeData:ages}).error.code,'escala_no_permitida'); assert.equal(lookupEquivalentAge({scaleId:'N-54 screening_areas',directScore:1,equivalentAgeData:ages}).error.code,'escala_no_permitida'); assert.equal(lookupEquivalentAge({scaleId:'battelle_total',directScore:999,equivalentAgeData:ages,maxScore:682}).error.code,'pd_invalida'); const dup=structuredClone(ages); dup.escalas['N-65 battelle_total'].registros.push({...dup.escalas['N-65 battelle_total'].registros[0]}); assert.equal(lookupEquivalentAge({scaleId:'battelle_total',directScore:0,equivalentAgeData:dup}).error.code,'coincidencia_multiple'); });
